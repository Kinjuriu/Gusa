name: gusa
description: "Gusa — always-on Braille/haptic accessibility assistant. Demo slice."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  vibration: ^3.1.3
  android_intent_plus: ^5.2.0
  installed_apps: ^1.6.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
